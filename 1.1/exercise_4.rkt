#reader(lib "htdp-beginner-reader.ss" "lang")((modname Exercise-1) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;model 
;(define (a-plus-abs-b a b)
; ((if (> b 0) + -) a b )

; there is a function a-plus-abs-b that takes input a and b, if b is positive, return + operator on the a and b argument, if b is negative, return - operator such that it because a - (-b) which is also a + b
